# StorageClass typo.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wrong-sc-pvc
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: fast-ssd