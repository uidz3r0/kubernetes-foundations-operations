apiVersion: batch/v1
kind: Job
metadata:
  name: basic-job

spec:
  template:
    spec:
      restartPolicy: Never

      containers:
      - name: worker
        image: busybox
        command:
        - sh
        - -c
        - |
          echo "Running job..."
          sleep 10
          echo "Job complete"