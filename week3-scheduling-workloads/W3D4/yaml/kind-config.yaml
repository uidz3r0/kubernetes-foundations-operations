# Use the same 3-node cluster pattern.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
  - role: control-plane

  - role: worker
    labels:
      zone: east

  - role: worker
    labels:
      zone: west