apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: aws-secretstore
spec:
  provider:
    aws:
      service: SecretsManager
      region: ap-southeast-2
      # Production use also requires authentication configuration,
      # such as IRSA, access keys, or another supported auth method.
