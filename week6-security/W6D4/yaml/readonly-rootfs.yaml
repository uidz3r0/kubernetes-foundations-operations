apiVersion: v1
kind: Pod
metadata:
  name: readonly
spec:
  containers:
    - name: alpine
      image: alpine:latest
      command: ["sleep", "3600"]

      securityContext:
        readOnlyRootFilesystem: true