apiVersion: v1
kind: Pod
metadata:
  name: nonroot
spec:
  containers:
    - name: alpine
      image: alpine:latest
      command: ["sleep", "3600"]
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000