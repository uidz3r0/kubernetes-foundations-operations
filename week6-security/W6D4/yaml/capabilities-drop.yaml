apiVersion: v1
kind: Pod
metadata:
  name: capabilities
spec:
  containers:
    - name: alpine
      image: alpine:latest
      command: ["sleep", "3600"]

      securityContext:
        capabilities:
          drop:
            - ALL