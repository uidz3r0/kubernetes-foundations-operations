apiVersion: v1
kind: Pod
metadata:
  name: rbac-test
  namespace: development
spec:
  serviceAccountName: developer-sa
  containers:
  - name: kubectl
    image: bitnami/kubectl:latest
    command:
    - sleep
    - "3600"